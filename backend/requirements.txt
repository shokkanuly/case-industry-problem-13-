# Edge Telemetry Backend
# Install: pip install -r requirements.txt

# Web Framework
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-multipart

# Numerics (also pulled in by opencv/ultralytics, pinned here for the
# case-engine layer and simulation-fallback paths)
numpy

# Environment
python-dotenv

# HTTP
requests

# Machine Learning & CV
ultralytics
huggingface_hub
opencv-python-headless

# Time-series & Message Broker
duckdb
paho-mqtt
influxdb-client

# Optional — face recognition for the Personnel tab (Case 13 live matching).
# Not required to run the app: without these, workers are still registered,
# just without a face embedding. Install separately if you want face matching
# (needs a Python version with onnxruntime wheels available, e.g. 3.11/3.12):
#   pip install insightface onnxruntime
